{
  "name": "ghost-high-fiver",
  "version": "0.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build --public-url ./ --no-source-maps --experimental-scope-hoisting index.html",
    "clean": "rm -rf dist",
    "assets:optimize": "for a in dist/*.png; do convert $a PNG8:$a.png8; mv $a.png8 $a; done;",
    "zip:adv": "./tools/advzip.sh && advzip -z -4 -i 60 dist/game.zip",
    "zip:create": "pushd dist/ && zip -X -r game.zip index.html *.png *.svg *.js && popd",
    "zip:measure": "echo $(echo \"13312 - $(wc -c < dist/game.zip)\" | bc) bytes remain",
    "zip": "npm run clean && NODE_ENV=production npm run build && npm run assets:optimize && npm run zip:create && npm run zip:adv && npm run zip:measure",
    "deploy": "npm run clean && npm run build && gh-pages -d dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.9.7",
    "typescript": "^3.0.3"
  },
  "dependencies": {
    "idb-keyval": "^3.1.0",
    "js13k-ecs": "^0.1.2",
    "pwacompat": "^2.0.7"
  },
  "//browserslist": "Just to prevent parcel from causing babel to transpile anything and injecting regenerator runtime. Typescript already handles this.",
  "browserslist": [
    "chrome 69"
  ]
}
